<Application x:Class="ImageCompression.Wpf.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ImageCompression.Wpf"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <!-- 기본 문자열 리소스(초기 로드 언어). 런타임에서 LocalizationManager가 교체 가능 -->
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Strings.ko.xaml" />
            </ResourceDictionary.MergedDictionaries>

        <!-- 공통 테마 색상 팔레트 -->
        <SolidColorBrush x:Key="AppBgBrush" Color="#F3F6FB" />
        <SolidColorBrush x:Key="CardBgBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="CardBorderBrush" Color="#DDE3EE" />
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2F6FEB" />
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#245BCE" />
        <SolidColorBrush x:Key="SubtleTextBrush" Color="#5B667A" />

        <!-- Window 기본 스타일: 폰트/배경/전역 텍스트 톤 -->
        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource AppBgBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="#1F2937" />
        </Style>

        <!-- GroupBox 공통 카드 스타일 -->
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,8,0,0" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="{StaticResource CardBgBrush}" />
        </Style>

        <!-- 입력 컨트롤 기본 스타일 -->
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="30" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#CAD3E2" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="30" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#CAD3E2" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <!-- 기본 버튼 스타일(라운드 사각형 + Hover/Disabled 상태) -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryDarkBrush}" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.55" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 보조(고스트) 버튼 스타일: 흰 배경/회색 테두리 -->
        <Style x:Key="GhostButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="White" />
            <Setter Property="Foreground" Value="#1F2937" />
            <Setter Property="BorderBrush" Value="#C8D0E0" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F2F5FB" />
                    <Setter Property="BorderBrush" Value="#B8C3D8" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 진행바 기본 스타일 -->
        <Style TargetType="ProgressBar">
            <Setter Property="Height" Value="18" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Background" Value="#E7ECF5" />
            <Setter Property="BorderBrush" Value="#D4DDEA" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <!-- DataGrid 기본 스타일 -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#D6DEEB" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="RowBackground" Value="White" />
            <Setter Property="AlternatingRowBackground" Value="#F8FAFD" />
            <Setter Property="GridLinesVisibility" Value="Horizontal" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="HorizontalGridLinesBrush" Value="#E7ECF5" />
            <Setter Property="VerticalGridLinesBrush" Value="#E7ECF5" />
        </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
